---
title: "工具与技能"
icon: Wrench
description: "一次性注册共享能力，然后为每个 Agent 启用合适的工具和技能。"
---

import imgToolsSkills from '@/public/images/tools-skills.png'
import imgMcpServer from '@/public/images/mcp-server.png'
import imgAgentToolsSkills from '@/public/images/agent-tools-skills.png'

**工具与技能**是组织级的能力库，收录你的 Agent 可以使用的能力。一次性注册 MCP 提供方、连接器或 Git 技能来源，然后明确选择哪些 Agent 可以获得它。新建的 Agent 初始的工具和技能允许列表为空。

<Image src={imgToolsSkills} alt="组织的连接器、MCP 服务器和技能" width={900} />

AgentConnect 把组织级管理与 Agent 级启用分开：

| 能力 | 添加或管理 | 为 Agent 启用 |
| --- | --- | --- |
| 连接器或自定义 MCP 服务器 | **工具与技能 → 连接器和 MCP 服务器** | Agent → **工具与技能 → 工具** |
| Git 技能来源 | **工具与技能 → 技能库** | Agent → **工具与技能 → 技能** |
| 托管技能 | **知识库 → 建议**，然后进入**技能库** | Agent → **工具与技能 → 技能** |

向组织库中添加内容不会自动把它提供给每个 Agent。

## 连接器和 MCP 服务器 [#connectors--mcp-servers]

打开**工具与技能**，使用**连接器和 MCP 服务器**注册一项上游能力：

- **添加连接器**会浏览可用的 OpenConnector 提供方，并引导完成它们的授权。
- **自定义 MCP 提供方**通过 URL 连接一个 HTTP MCP 端点，可选配置上游请求头。

<Image src={imgMcpServer} alt="注册自定义 MCP 服务器" width={520} />

添加提供方时，选择**所有人**或**指定成员**作为团队可见范围。请求头的值在保存后是只写的，Agent 获得访问权限时看不到存储的凭证。

只有当 Agent 的 Daemon 和所选 Runtime 支持提供方的传输方式时，该提供方才会显示为可供该 Agent 使用。注册提供方只是使它可供选择；不会为任何 Agent 打开它。

删除提供方之前，请先在每个启用了它的 Agent 上关闭它。

### 添加连接器 [#add-a-connector]

1. 打开**工具与技能 → 连接器和 MCP 服务器**，选择**添加连接器**。
2. 搜索目录或按类别筛选，然后选择一个服务。
3. 选择授权方式。API 密钥、自定义凭证和无需授权的连接会立即保存；OAuth 会打开一个弹窗，到提供方处完成授权。
4. 为连接命名。最多 32 个字符，可使用字母、数字、`_` 或 `-`，且以字母或数字开头。名称在组织内必须唯一，并且不能与任何 Agent 已启用的 MCP 服务器名称相同。
5. 选择它的团队可见范围，然后创建。

之后该连接的行为与**连接器和 MCP 服务器**中的其他条目一样：在你为某个 Agent 启用它之前，它保持关闭。

当令牌过期或上游密钥轮换时，打开该连接并选择**重新连接连接器**或**编辑 API 密钥**。连接器的端点和请求头由系统代为管理，不能手动编辑，因此重新连接就是刷新其凭证的方式。

如果目录中缺少你期望的服务，通常是以下两种情况之一。AgentConnect 直接集成的服务——GitHub、GitLab、Linear、Slack、Telegram、Discord 和 Lark / 飞书——默认不出现在连接器目录中，而是使用它们各自的集成。仅通过 OAuth 授权的服务，只有在部署中为它们配置了 OAuth 客户端之后才会出现——自托管部署请参阅[连接器网关](/self-hosting/connector-gateway#oauth-providers)。

同时提供 OAuth 和其他授权方式的服务无论如何都会列出。只有 OAuth 这一选项会在其客户端配置好之前被隐藏，因此你可能会在目录中看到某个服务的授权选项比提供方实际支持的要少。

## 技能库 [#skills-library]

技能库包含两种生命周期不同的来源类型：

| 来源类型 | 如何进入技能库 | 生命周期 |
| --- | --- | --- |
| **Git 技能来源** | 搜索 skills.sh 或导入 GitHub 仓库 | 可编辑；更新会重新安装已启用的来源 |
| **托管技能** | 由所有者接受一条建议 | 不可变的修订版本；可归档或恢复 |

托管技能和 Git 来源虽然共用一个库，但始终有清晰的标签区分。向库中添加技能永远不会自动启用它。

### 从 skills.sh 安装 [#install-from-skillssh]

当你知道想要的能力但不知道它的仓库时，使用公开的 [skills.sh](https://skills.sh) 注册表：

1. 打开**工具与技能 → 技能库**，选择**从 skills.sh 安装**。
2. 按技能名称搜索并选择一个结果。
3. 可选地更改它在库中的名称，选择它的团队可见范围，然后选择**安装**。

每个结果都只会从其 `owner/repo` 来源注册那一个技能。它会成为组织库中一个普通的 Git 技能来源，并在你为某个 Agent 启用之前保持禁用。如果无法访问注册表，请稍后重试，或者用你已知的仓库通过**从 GitHub 导入**添加。

### 从 GitHub 导入 [#import-from-github]

![将 Git 仓库导入为技能来源](/images/skills-import.png)

1. 打开**工具与技能 → 技能库**，选择**从 GitHub 导入**。
2. 输入 `owner/repo` 或 GitHub 仓库 URL。仓库中的每个技能应各自位于一个包含 `SKILL.md` 文件的文件夹中。
3. 可选地设置显示名称、分支/标签/提交**引用**、**子目录**，或具体的**技能**列表。技能留空则包含所有发现的技能。
4. 选择它的团队可见范围，然后选择**导入**。

目前，注册表安装和直接导入都只支持公开的技能仓库。如果你设置了**子目录**，请同时提供**引用**，除非 AgentConnect 能通过组织的 GitHub App 解析出仓库的默认分支。

AgentConnect 会在拥有 Agent 的 Daemon 上安装已启用的技能。当你需要在多个 Daemon 之间获得可复现的内容时，请固定到某个标签或提交。删除来源之前，请先在每个使用它的 Agent 上禁用它。

## 为 Agent 启用工具和技能 [#enable-tools-and-skills-for-an-agent]

打开 Agent，选择它的**工具与技能**标签页。

<Image src={imgAgentToolsSkills} alt="Agent 的工具与技能标签页：按 Agent 开启的 MCP 服务器和技能来源" width={820} />

在**工具**下，打开该 Agent 应获得的 MCP 提供方。列表把组织注册的提供方与 Agent 的 Daemon 上报的服务器合并在一起，再按所选 Runtime 的能力进行筛选。

在**技能**下：

- 启用一个已批准的托管技能，以使用它当前的不可变修订版本；
- 启用整个 Git 来源；或者
- 展开一个 Git 来源，选择其中发现的单个技能。

已归档的托管技能无法启用。

### 从工作区加载 [#loaded-from-the-workspace]

Agent 的**工具与技能**标签页还会显示其已准备好的工作区实际能加载的技能。每一行都会标明来源：**梦境**、**托管**、**Git 来源**或**仓库**，这样你可以区分组织管理的能力与随项目直接提交的技能。这个实时列表在工作区准备完成后才会出现，并且在拥有该 Agent 的 Daemon 离线时不可用。

## 可见范围与角色 [#visibility-and-roles]

MCP 提供方和 Git 技能来源各自拥有独立的[团队可见范围](/operate-govern/permissions-overview/team-visibility)：**所有人**或**指定成员**。指定成员会把访问权限授予所选的当前组织成员以及每一位组织所有者。至少要保留一名明确选中的成员。创建时当前用户默认被选中，在选中其他人之后可以把自己替换掉；所有者通过其角色保留访问权限。协作者可以创建和编辑他们有权访问的资源，而查看者是只读的。

托管技能的批准、修订版本治理以及归档或恢复操作都需要组织所有者。

团队可见范围控制哪些人可以发现和管理某个资源。Agent 的明确启用控制该能力是否到达 Agent 进程；这两项检查是相互独立的。
